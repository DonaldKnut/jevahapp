# API base used by the web app. Routes are mounted under /api, and the app
# calls paths like `/auth/login`, so the base must end with /api.
#
# Production (Contabo): https://api.jevahapp.com/api
# Local backend:        http://localhost:4000/api
VITE_API_URL=https://api.jevahapp.com/api
